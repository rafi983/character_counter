import LetterDensityList from '../LetterDensityList/LetterDensityList';

const LetterDensity = () => {
  return (
    <div className="mt-6">
      <h2 className="text-Heading-24 font-semibold text-counter-neutral-900 dark:text-white">
        Letter Density
      </h2>
      <LetterDensityList />
    </div>
  );
};

export default LetterDensity;
